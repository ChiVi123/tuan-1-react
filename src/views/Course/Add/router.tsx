import { RouteObject } from 'react-router-dom';
import AddCourse from '.';

const addCourseRouter: RouteObject = {
    path: '/course/add',
    Component: AddCourse,
};

export default addCourseRouter;
